import React from 'react';

const Header = ({title}) => (
    <React.Fragment>
      <h1 className="Listado-de-Prestamos">
          {title}
      </h1>
    </React.Fragment>
  )
  
  export default Header
  